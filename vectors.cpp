#include <iostream>
#include <vector>
#include <any>

using namespace std;

// Vector practice

int main() {
    vector<int> numbers = {1,2,3,4};
    for (int i = 0; i <  numbers.size(); i++) {
        
    }
}